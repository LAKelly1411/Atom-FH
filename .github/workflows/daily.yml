name: PA ATOMIC — Daily Food Hygiene Pipeline

on:
  schedule:
    # 06:30 UTC every day — FSA data typically updates overnight
    - cron: '30 6 * * *'
  workflow_dispatch:
    inputs:
      date_override:
        description: 'Run for a specific date (YYYY-MM-DD). Leave blank for today.'
        required: false
        default: ''
      dry_run:
        description: 'Dry run — skip email sending'
        type: boolean
        required: false
        default: false

jobs:
  run-pipeline:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    # Required so the job can commit data back to the repo
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Restore database from cache
        uses: actions/cache/restore@v4
        with:
          path: data/fhrs.db
          # Unique key per run so we always write a fresh entry;
          # restore-keys prefix picks up the most recent saved DB.
          key: fhrs-db-${{ github.run_id }}
          restore-keys: fhrs-db-

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run pipeline
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          SMTP_HOST:         ${{ secrets.SMTP_HOST }}
          SMTP_PORT:         ${{ secrets.SMTP_PORT }}
          SMTP_USER:         ${{ secrets.SMTP_USER }}
          SMTP_PASS:         ${{ secrets.SMTP_PASS }}
          SMTP_FROM:         ${{ secrets.SMTP_FROM }}
          DIGEST_RECIPIENTS: ${{ secrets.DIGEST_RECIPIENTS }}
        run: |
          ARGS=""
          [ -n "${{ github.event.inputs.date_override }}" ] && ARGS="$ARGS --date ${{ github.event.inputs.date_override }}"
          [ "${{ github.event.inputs.dry_run }}" = "true" ]  && ARGS="$ARGS --dry-run"
          python run.py $ARGS --verbose

      - name: Save database to cache
        # Always save — even if the pipeline step failed part-way through,
        # we want to preserve whatever was written to the DB.
        if: always()
        uses: actions/cache/save@v4
        with:
          path: data/fhrs.db
          key: fhrs-db-${{ github.run_id }}

      - name: Commit digest and notifications back to repo
        run: |
          git config user.name  "PA ATOMIC Bot"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # Stage HTML outputs only — DB is in cache, not git
          git add output/digest_*.html
          git add output/notifications.html
          git add output/insights.html

          # Only commit if there's actually something new
          if git diff --staged --quiet; then
            echo "Nothing changed — no commit needed."
          else
            git commit -m "chore: daily pipeline $(date -u +%Y-%m-%d)"
            git push
          fi

      - name: Upload digest as artefact
        uses: actions/upload-artifact@v4
        with:
          name: digest-${{ github.run_id }}
          path: output/digest_*.html
          retention-days: 90
